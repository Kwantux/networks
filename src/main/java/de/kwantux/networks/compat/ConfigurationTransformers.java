package de.kwantux.networks.compat;

import de.kwantux.config.SimpleConfig;

public final class ConfigurationTransformers {

    public static void generalConfigTransformers(SimpleConfig config) {
        // Transformations are not supported in SimpleConfig
        // Users will need to manually update their config files when upgrading
    }

    public static void recipesTransformers(SimpleConfig config) {
        // Transformations are not supported in SimpleConfig
        // Users will need to manually update their config files when upgrading
    }
}
